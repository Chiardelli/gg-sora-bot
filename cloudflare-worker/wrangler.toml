name = "gg-heartbeat-telegram-relay"
main = "src/index.js"
compatibility_date = "2024-11-01"

[vars]
GITHUB_REPO = "Chiardelli/gg-heartbeat"

# TELEGRAM_WEBHOOK_SECRET e GITHUB_TOKEN são segredos — não vão aqui.
# Configure com: wrangler secret put TELEGRAM_WEBHOOK_SECRET
#                wrangler secret put GITHUB_TOKEN
